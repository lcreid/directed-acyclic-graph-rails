<%# When you're rendering from children index, you render the child. %>
<%= render partial: "edge/index",
           locals: {
             css_class: "js-children",
             method: :child_ids,
             post_path: @person.persisted? ? children_path(@person.id) : "#",
             relatives: @person.ancestors
           } %>
